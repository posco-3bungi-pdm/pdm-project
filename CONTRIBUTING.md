# 함께 작업하는 방법

## 1. Issue부터 만들기

`Issues → New issue`에서 작업·오류·실험 중 하나를 고릅니다. 목적, 해야 할 일, 완료 기준을 적고 라벨과 마일스톤을 지정합니다. Write 이상 권한을 가진 팀원이 만든 Issue는 비어 있는 담당자 칸에 작성자가 자동 배정됩니다. 실제 담당자가 다르면 변경합니다.

## 2. 작업 브랜치 만들기

```bash
git switch main
git pull --ff-only
git switch -c feat/12-data-quality
```

브랜치는 `feat/번호-설명`, `fix/번호-설명`, `docs/번호-설명`, `chore/번호-설명`을 권장합니다. 자동화가 준비하는 브랜치는 `codex/` 접두사를 사용합니다. 같은 노트북을 동시에 수정하지 말고 Issue에서 담당 파일을 정합니다.

## 3. 변경과 검증

커밋은 `feat: 코일 단위 특징 생성`, `fix: 시간 분할 경계 수정`처럼 무엇이 달라졌는지 적습니다. 개인 수업 저장소의 날짜 중심 커밋 규칙과 이 팀 저장소의 규칙은 별개입니다.

```bash
git add src/features.py
git commit -m "feat: 코일 단위 특징 생성"
git push -u origin feat/12-data-quality
```

원본 데이터·비밀번호·토큰·`.env`는 올리지 않습니다. 노트북 출력은 지우고 커밋하며, 공유할 결과는 검토한 그림과 요약 문서로 남깁니다.

## 4. PR과 리뷰

- PR 제목에 변경 목적을 적고 본문에 `Closes #12`처럼 Issue를 연결합니다.
- 실행 명령과 실제 확인 결과를 적습니다. 실행하지 않은 검사는 통과했다고 쓰지 않습니다.
- **작성자 외 동료 3명 이상 승인**, 필수 CI 통과, 미해결 대화 없음이 병합 조건입니다.
- 새 커밋을 올리면 기존 승인이 무효화됩니다. 최신 변경을 다시 검토받습니다.
- 노트북 형식 검사 통과는 모델 검증 통과를 뜻하지 않습니다. 시간·그룹 누수와 해석은 리뷰에서 확인합니다.
- 최종 병합은 `Squash and merge`. 병합 후 작업 브랜치는 자동 삭제됩니다.

## 5. 진행 상황

현재는 Issue·라벨·마일스톤으로 진행 상황을 관리합니다. Projects 보드를 도입하면 Backlog → Ready → In progress → In review → Done 순으로 이동합니다. 날짜는 팀의 실제 일정이 정해진 뒤 입력합니다. 역할은 책임 범위이며 특정 사람만 코드를 작성한다는 뜻이 아닙니다.
